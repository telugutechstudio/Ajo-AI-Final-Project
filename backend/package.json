{
  "name": "ajo-ai-backend",
  "version": "1.0.0",
  "description": "Backend server for Ajo AI Technologies Scribe",
  "main": "server.js",
  "scripts": {
    "preinstall": "npm cache clean --force",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "reinstall": "npm cache clean --force && npx rimraf ./node_modules ./package-lock.json && npm install"
  },
  "dependencies": {
    "@google/genai": "^1.9.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.3",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.3",
    "rimraf": "^5.0.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
